export function SettingsPage() {
  return (
    <div className="p-6">
      <h1 className="mb-2 text-xl font-semibold text-wa-dark">
        Admin settings
      </h1>
      <p className="text-sm text-wa-muted">
        Platform-level switches will live here later (e.g. require name, allow
        registration, OTP TTL).
      </p>
    </div>
  );
}
